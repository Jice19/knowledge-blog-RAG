# AI 智识博客 — 博客 MVP 骨架 · 方案计划书

> 版本：v1.0（2026-08）
> 阶段：Phase 1 博客 MVP（不含 RAG，RAG 属 Phase 2）
> 作者背景：前端开发转全栈（React/TS 为既有主场技能），学习重点在后端 Java 与后续 RAG/AI。
> 目标：搭建一个「后端 SpringBoot + 前端 React」可运行、可扩展的博客项目骨架，作为 RAG 知识库问答的前置底座。

---

## 1. 项目定位与目标

### 1.1 本次 MVP 要做什么
- 一个**前后端分离**的博客系统骨架，能跑通「登录 → 发布文章 → 浏览文章」完整闭环。
- 后端：SpringBoot 3 分层架构，含统一返回、全局异常、JWT 鉴权、MyBatis-Plus 基础 CRUD。
- 前端：React 18 + TypeScript + Vite，含博客前台 + 管理后台两个入口。

### 1.2 本次 MVP 不做什么（明确排除）
- ❌ 不做 RAG / 向量检索 / 大模型问答（Phase 2）
- ❌ 不做 RabbitMQ 异步任务（Phase 3）
- ❌ 不做 BM25 / 重排序 / 缓存优化（Phase 3）
- ❌ 不做评论、点赞、关注等社交功能
- ✅ 骨架的**扩展点预留**：文章表结构、包结构、API 设计都为后续「文章→切片→向量化」留好接口。

> 原则：**先跑通一个干净、规范、可扩展的骨架，再往上叠 RAG。** 不要在第一阶段就引入 Milvus/RabbitMQ 等重组件。

---

## 2. 技术选型与版本

| 层 | 技术 | 版本 | 说明 |
|---|---|---|---|
| 后端语言 | Java | 17（LTS） | 与简历一致 |
| 后端框架 | Spring Boot | 3.4.x | 3.x 要求 Java 17+ |
| ORM | MyBatis-Plus | 3.5.x | 黑马已学，零成本 |
| 数据库 | MySQL | 8.0 | 黑马已学 |
| 缓存 | Redis | 7.x | 黑马已学，MVP 仅用于 Token 白名单/后续扩展 |
| 鉴权 | JWT（jjwt） | 0.12.x | 轻量、无状态 |
| 构建 | Maven | 3.9 | |
| 前端框架 | React | 18.x | 与简历一致 |
| 前端语言 | TypeScript | 5.x | |
| 构建工具 | Vite | 6.x | 秒级热更新 |
| 路由 | React Router | 6.x | |
| UI 组件 | Ant Design | 5.x | 后台管理开箱即用 |
| Markdown 编辑 | @uiw/react-md-editor | - | 后台富文本 |
| Markdown 渲染 | react-markdown + remark-gfm | - | 前台渲染 |
| 请求 | Axios | 1.x | 封装拦截器 |
| 状态管理 | Zustand | 5.x | 轻量，存登录态 |

> 说明：MVP 阶段暂不引入 React Query，先用 Axios + 简单 hooks，降低心智负担；等业务复杂再上。

---

## 3. 仓库目录结构（Monorepo）

```
ai-knowledge-blog/
├── 方案计划书.md              # 本文档
├── README.md                  # 项目说明 + 启动方式
├── .gitignore
├── docker-compose.yml         # MySQL + Redis（可选，本地开发用）
├── docs/
│   └── api.md                 # 接口文档
├── backend/                   # Spring Boot 后端
└── frontend/                  # React 前端
```

### 3.1 后端目录结构

```
backend/
├── pom.xml
└── src/main/
    ├── java/com/jice19/blog/
    │   ├── BlogApplication.java
    │   ├── common/                    # 通用层
    │   │   ├── Result.java            # 统一返回体
    │   │   ├── ResultCode.java        # 状态码枚举
    │   │   ├── PageResult.java        # 分页返回体
    │   │   └── exception/
    │   │       ├── BusinessException.java
    │   │       └── GlobalExceptionHandler.java
    │   ├── config/                    # 配置层
    │   │   ├── MybatisPlusConfig.java # 分页插件、自动填充
    │   │   ├── WebMvcConfig.java      # CORS、拦截器注册
    │   │   └── RedisConfig.java
    │   ├── security/                  # 鉴权层
    │   │   ├── JwtUtil.java
    │   │   ├── JwtAuthInterceptor.java
    │   │   └── UserContext.java       # ThreadLocal 存当前用户
    │   ├── controller/
    │   │   ├── AuthController.java
    │   │   ├── ArticleController.java   # 前台公开接口
    │   │   ├── CategoryController.java
    │   │   ├── TagController.java
    │   │   └── admin/                  # 后台管理接口
    │   │       ├── AdminArticleController.java
    │   │       ├── AdminCategoryController.java
    │   │       └── AdminTagController.java
    │   ├── service/ 及 service/impl/
    │   │   ├── AuthService
    │   │   ├── ArticleService
    │   │   ├── CategoryService
    │   │   └── TagService
    │   ├── mapper/
    │   ├── entity/
    │   │   ├── User.java
    │   │   ├── Article.java
    │   │   ├── Category.java
    │   │   ├── Tag.java
    │   │   └── ArticleTag.java
    │   ├── dto/                       # 入参对象
    │   │   ├── LoginDTO.java
    │   │   ├── ArticleDTO.java
    │   │   └── ...
    │   └── vo/                        # 出参对象
    │       ├── LoginVO.java
    │       ├── ArticleVO.java
    │       └── ...
    └── resources/
        ├── application.yml
        ├── application-dev.yml
        ├── mapper/                    # MyBatis XML（复杂 SQL）
        └── db/
            └── schema.sql             # 建表语句 + 初始数据
```

### 3.2 前端目录结构

```
frontend/
├── package.json
├── vite.config.ts
├── tsconfig.json
├── index.html
└── src/
    ├── main.tsx
    ├── App.tsx
    ├── api/                   # Axios 实例 + 各模块接口
    │   ├── http.ts
    │   ├── auth.ts
    │   ├── article.ts
    │   ├── category.ts
    │   └── tag.ts
    ├── router/
    │   └── index.tsx          # 前台/后台路由
    ├── stores/
    │   └── auth.ts            # Zustand 登录态
    ├── types/                 # TS 类型定义
    ├── components/
    │   ├── MarkdownViewer.tsx
    │   └── AuthGuard.tsx      # 路由守卫
    ├── pages/
    │   ├── blog/              # 前台
    │   │   ├── HomePage.tsx      # 文章列表
    │   │   ├── ArticlePage.tsx   # 文章详情
    │   │   └── CategoryPage.tsx  # 分类浏览
    │   └── admin/             # 后台
    │       ├── LoginPage.tsx
    │       ├── DashboardPage.tsx
    │       ├── ArticleManagePage.tsx
    │       ├── ArticleEditPage.tsx
    │       ├── CategoryManagePage.tsx
    │       └── TagManagePage.tsx
    └── styles/
```

---

## 4. 数据库设计（MVP 最小集）

> 关键点：`article` 表为 Phase 2 的「切片 → 向量化」预留字段，如 `status`（发布态才进向量库）、`summary`（后续可用于文章级摘要）。

```sql
-- 用户表
CREATE TABLE `user` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `username` VARCHAR(50) NOT NULL UNIQUE,
  `password` VARCHAR(100) NOT NULL,        -- BCrypt 加密
  `nickname` VARCHAR(50),
  `avatar` VARCHAR(255),
  `role` VARCHAR(20) DEFAULT 'ADMIN',      -- 预留，MVP 单管理员
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 分类表
CREATE TABLE `category` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `name` VARCHAR(50) NOT NULL UNIQUE,
  `slug` VARCHAR(50) NOT NULL UNIQUE,
  `sort` INT DEFAULT 0,
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 标签表
CREATE TABLE `tag` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `name` VARCHAR(50) NOT NULL UNIQUE,
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 文章表（Phase 2 向量化的核心源表）
CREATE TABLE `article` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `title` VARCHAR(200) NOT NULL,
  `summary` VARCHAR(500),
  `content` LONGTEXT NOT NULL,             -- Markdown 原文
  `cover` VARCHAR(255),
  `category_id` BIGINT,
  `author_id` BIGINT NOT NULL,
  `status` TINYINT DEFAULT 0,              -- 0草稿 1已发布（发布才进向量库）
  `view_count` INT DEFAULT 0,
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  KEY idx_category (category_id),
  KEY idx_status (status)
);

-- 文章-标签关联表
CREATE TABLE `article_tag` (
  `article_id` BIGINT NOT NULL,
  `tag_id` BIGINT NOT NULL,
  PRIMARY KEY (article_id, tag_id)
);
```

---

## 5. 后端设计要点

### 5.1 统一返回体
```json
{ "code": 0, "message": "success", "data": { ... } }
```
- `code=0` 成功；非 0 为业务错误码（如 401 未登录、40001 用户名密码错误）。

### 5.2 核心 API 清单（RESTful）

| 方法 | 路径 | 说明 | 鉴权 |
|---|---|---|---|
| POST | `/api/auth/login` | 登录，返回 JWT | 否 |
| GET | `/api/auth/me` | 获取当前用户信息 | 是 |
| GET | `/api/articles` | 文章分页列表（公开） | 否 |
| GET | `/api/articles/{id}` | 文章详情 + 浏览量自增 | 否 |
| GET | `/api/categories` | 分类列表 | 否 |
| GET | `/api/tags` | 标签列表 | 否 |
| GET | `/api/categories/{slug}/articles` | 按分类浏览文章 | 否 |
| POST | `/api/admin/articles` | 创建文章 | 是 |
| PUT | `/api/admin/articles/{id}` | 更新文章 | 是 |
| DELETE | `/api/admin/articles/{id}` | 删除文章 | 是 |
| GET | `/api/admin/articles` | 后台文章列表（含草稿） | 是 |
| POST/PUT/DELETE | `/api/admin/categories` | 分类管理 | 是 |
| POST/PUT/DELETE | `/api/admin/tags` | 标签管理 | 是 |

> 约定：`/api/admin/**` 全部经过 JWT 拦截器校验；`/api/**` 前台接口公开。

### 5.3 中间件与横切能力（黑马已学，直接落地）
- **JWT 鉴权**：登录发放 Token，前端存 `localStorage`，请求头 `Authorization: Bearer <token>`，后端拦截器解析 + ThreadLocal 存用户。
- **全局异常**：`@RestControllerAdvice` 统一捕获业务异常/参数校验异常/系统异常。
- **参数校验**：`spring-boot-starter-validation` + DTO 注解。
- **MyBatis-Plus 自动填充**：`create_time`/`update_time` 用 MetaObjectHandler 自动填充。
- **分页插件**：PaginationInnerInterceptor。

---

## 6. 前端设计要点

### 6.1 页面清单

**前台（/）**
| 页面 | 路由 | 功能 |
|---|---|---|
| 首页 | `/` | 文章列表（分页、分类筛选、标签） |
| 文章详情 | `/article/:id` | Markdown 渲染、分类/标签、浏览量 |
| 分类浏览 | `/category/:slug` | 按分类查看文章 |

**后台（/admin）**
| 页面 | 路由 | 功能 |
|---|---|---|
| 登录 | `/admin/login` | 登录跳转后台 |
| 仪表盘 | `/admin` | 文章数、分类数统计 |
| 文章列表 | `/admin/articles` | 表格 + 分页 + 删除 |
| 文章编辑 | `/admin/articles/new`、`/admin/articles/:id/edit` | Markdown 编辑器 + 发布/存草稿 |
| 分类管理 | `/admin/categories` | CRUD |
| 标签管理 | `/admin/tags` | CRUD |

### 6.2 关键交互
- **路由守卫**：`AuthGuard` 组件，未登录访问 `/admin/**` 重定向到 `/admin/login`。
- **Axios 拦截器**：请求自动带 Token；响应统一解包 `{code, message, data}`，`code=401` 时跳登录。
- **Markdown**：编辑用 `@uiw/react-md-editor`（支持预览、代码高亮），展示用 `react-markdown + remark-gfm + 代码高亮插件`。
- **登录态**：Zustand 持久化到 localStorage。

---

## 7. 开发环境与启动方式

### 7.1 前置依赖
- JDK 17、Maven 3.9、Node 18+、pnpm（或 npm）、Docker（可选）

### 7.2 启动步骤
```bash
# 1. 启动 MySQL + Redis（Docker，可选）
docker compose up -d

# 2. 后端
cd backend
mvn spring-boot:run   # 默认 8080

# 3. 前端
cd frontend
pnpm install
pnpm dev              # 默认 5173，/api 代理到 8080
```

### 7.3 前端代理配置
`vite.config.ts` 中 `/api` 代理到 `http://localhost:8080`，解决跨域。

---

## 8. 里程碑计划（Phase 1 内部细分）

| 里程碑 | 交付物 | 预估 |
|---|---|---|
| M1 后端骨架 | 项目可启动、统一返回/异常、MyBatis-Plus 配置、schema.sql | 第 1 周 |
| M2 文章 CRUD | entity/mapper/service/controller 全链路 + 分页 | 第 1 周 |
| M3 JWT 鉴权 | 登录接口、拦截器、ThreadLocal、参数校验 | 第 2 周 |
| M4 前端骨架 | Vite + React + TS + 路由 + AntD 引入 | 第 2 周 |
| M5 前台页面 | 首页列表、文章详情、分类浏览、Markdown 渲染 | 第 3 周 |
| M6 后台页面 | 登录、文章管理、分类/标签管理、Markdown 编辑器 | 第 3~4 周 |
| M7 联调与部署 | 前后端联调、README、Docker Compose、Git 初始化 | 第 4 周 |

> 共约 4 周，正好对应评估报告里的「Phase 1：博客 MVP」。
>
> 作者是前端出身，M4~M6 前端部分预期可压缩；M1~M3 后端与 M7 联调是学习重点，建议多投入。

---

## 9. 验收标准（Definition of Done）

- [ ] `docker compose up` + 两条命令能一键启动，本地可访问前台与后台
- [ ] 用管理员账号登录后，能发布一篇 Markdown 文章（含分类、标签、封面）
- [ ] 前台首页能分页浏览文章，点击进入详情并正确渲染 Markdown（含代码高亮）
- [ ] 未登录访问 `/admin/**` 自动跳转登录
- [ ] 所有接口统一返回 `{code, message, data}`，异常有兜底处理
- [ ] `docs/api.md` 有完整接口文档
- [ ] 代码提交到 Git，README 含架构说明与启动方式

---

## 10. 风险与备注

1. **后端 Java 是学习重点**：M1~M3 后端骨架是你转全栈的核心，会明显慢于前端，属正常现象；React/前端是你的主场，M4~M6 应能快速完成，别在 UI 细节上耗太多时间，把精力留给后端与后续 RAG。
2. **Spring Boot 2 → 3 的差异**：黑马若教的是 2.x，注意 `javax.*` → `jakarta.*` 包名变化、`spring.factories` 等配置差异。
3. **版本兼容**：Ant Design 5 需 React 16.9+，与 React 18 兼容良好；若遇 `react-markdown` v9 的 ESM 问题，可退到 v8。
4. **扩展点预留是重点**：`article.status`（发布态）、`article.content`（Markdown 原文）是 Phase 2 切片的输入，务必保持字段语义清晰，不要现在就把内容搞成 HTML。

---

## 11. Phase 2 前瞻设计：内容导入与解析

> 本章为 Phase 2 设计前瞻，Phase 1（博客 MVP）暂不实现，先定方案避免返工。`article.status`（发布态）与 `article.content`（Markdown 原文）字段已为本章预留。

### 11.1 完整导入流水线

```
上传(zip/单文件/Git仓库/PDF)
  → 文件类型分发 → DocumentParser 解析成统一结构(标题 + 正文Markdown + 元数据)
  → 写入 article 表(status=草稿)
  → Markdown AST 解析 → 按标题切片 → 生成 chunk 列表
  → [异步:RabbitMQ] 逐 chunk Embedding 向量化 → 写入向量库
```

解析与切片是 CPU 密集且耗时（尤其批量/PDF），放 RabbitMQ 异步队列，与「异步向量化任务队列」对应。

### 11.2 Markdown 按标题结构化切片（核心）

- **用 AST 解析，不用正则**：代码块里的 `#` 会被正则误判。Java 用 commonmark-java 或 flexmark-java。
- **切片边界**：以 `##`（二级标题）为主边界，每个 chunk = 「标题路径 + 该章节正文」；正文过长再按段落二次切。
- **chunk 元数据**（答案引用溯源的根基）：
  - `articleId` / `articleTitle`
  - `headingPath`（如 `Spring Boot / 第一个接口 / 返回 JSON`）
  - `chunkIndex`（章节顺序）
  - `hasCodeBlock`
- **Frontmatter 批量解析**：用 snakeyaml 解析 YAML 头（title/category/tags），批量导入时自动填充文章元数据。
- **批量导入入口**：上传 zip 或粘贴 Git 仓库地址，遍历所有 `.md` 文件。这是「300+ 技术文章」的唯一可行落地方式。

### 11.3 文件上传与多格式解析

抽象 `DocumentParser` 接口，各格式归一成统一结构：

```java
interface DocumentParser {
    boolean supports(String filename);
    ParsedDocument parse(byte[] content);   // → { title, content(Markdown), category, tags, summary }
}
```

| 格式 | 解析器 | 优先级 |
|---|---|---|
| .md / .txt | 原生（直接读） | 最高 |
| .html | 老博客迁移（Jsoup） | 中 |
| .docx | Apache POI | 中 |
| .pdf | MinerU（见 11.4） | 低（兼容能力） |

> 技术博客场景优先引导用 Markdown，质量最高；PDF 作为兼容能力。

### 11.4 PDF 解析方案：MinerU（已调研）

**是什么**：OpenDataLab（上海人工智能实验室）开源的高精度 PDF 解析引擎，PDF → 结构化 Markdown + JSON，面向 LLM/RAG。

**能力与原理**：
- 版面分析、表格识别（→Markdown 表格）、公式识别（→LaTeX）、OCR（PaddleOCR，扫描件可用）、中英文友好
- 流水线：版面分析 → OCR → 公式识别(UniMERNet) → 表格识别(StructEqTable) → Markdown + JSON

**选它的核心原因**：输出带 `#/##` 标题层级的结构化 Markdown，**PDF 与原生 MD 走同一套「按标题切片」流水线**，不需要两套逻辑。

**版本与 License**：
- 迭代快：2.5 引入 1.2B 大模型，当前 3.x
- 代码 AGPL-3.0，模型另有各自授权
  - ✅ 个人学习项目：无碍
  - ✅ 独立服务 + API 调用：Java 代码是独立程序，不被传染（不改 MinerU 源码即可）
  - ⚠️ 修改 MinerU 源码 → 修改部分需开源
  - ⚠️ 未来商用闭源 SaaS → 谨慎（备选 Docling，Apache 2.0）

**资源**：模型几 GB，GPU 推荐，CPU 慢且易 OOM。学习期三选一：本地 GPU / 云端 GPU / MinerU 托管 API。

**竞品**：Docling（Apache 2.0，质量接近）、Marker（轻但中文弱）、PDFBox（纯文本，无结构）。

### 11.5 Python 服务与 Java 后端集成

**本质**：两个进程 + HTTP 通信（与前端调后端同款模式），不是微服务，无需 Spring Cloud。

```
① React 前端 :5173 ──HTTP──> ② Java 后端 :8080 ──HTTP──> ③ MinerU(Python) :8000
```

**集成三方案**：

| 方案 | 做法 | 评价 |
|---|---|---|
| A. 独立 API 服务（推荐） | 启动 MinerU 官方 API 服务，Java 用 WebClient 调 HTTP | 最干净、解耦 |
| B. CLI 子进程 | Java ProcessBuilder 调 mineru 命令 | 小规模可用，生产脏 |
| C. 托管 API | 官方/第三方云 API | 零部署，花钱依赖外部 |

**Java 侧落点**：一个 `PdfParseService`（封装 WebClient 调用，约 30 行）+ 一个 controller 接收上传 + RabbitMQ 异步消费。**不写 Python 业务代码**，只部署 + 启动 MinerU 官方服务。

**目录组织**：
- 开发期：MinerU 当外挂服务（像 MySQL），不进仓库，单独启动，Java 配置写 `http://localhost:8000`
- 上线期：`services/mineru/` 放 Dockerfile + requirements.txt（锁定版本），`docker-compose.yml` 统一编排

**三级上手路线**：L0 托管 API（零部署）→ L1 本地起 API 服务 → L2 Docker Compose 容器化。

**参考**：[MinerU GitHub](https://github.com/opendatalab/MinerU) · [MinerU 2.5 部署](https://developer.aliyun.com/article/1705323) · [License 澄清](https://github.com/opendatalab/MinerU/discussions/2863) · [MinerU vs Docling vs Marker 对比](https://juejin.cn/post/7636666943860572170)

---

## 附：与整体项目（RAG 全链路）的关系

本 MVP 是「AI 智识博客」的第一块地基。后续里程碑：
- **Phase 2**：文章发布 → 切片 → Embedding → Milvus 入库 → 向量检索 → LLM 问答 → SSE 流式
- **Phase 3**：RabbitMQ 异步向量化、BM25+向量双路召回、Rerank、Redis 缓存、多轮改写
- **Phase 4**：评测体系、后台模型配置、上线部署

当前骨架的 `article` 表、分层架构、鉴权体系将直接复用到后续所有阶段。
